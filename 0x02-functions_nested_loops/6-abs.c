#include "main.h"

/**
 * _abs - compute absolute value of an integer
 *
 * @int: the value we're computing
 *
 * Return: void
 */

int _abs(int)
{
int n;

if (n >= 0)
	return (n);
else
	return (-n);

}
